{
  "scripts": {
    "start": "cd hospital-project/backend && npm install && node db-setup.js && node server.js",
    "start:frontend": "cd hospital-project/frontend && npm install && npm start",
    "start:backend": "cd hospital-project/backend && npm install && node db-setup.js && node server.js",
    "install:all": "cd hospital-project/backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^7.13.2",
    "react-scripts": "^5.0.1"
  }
  
}
